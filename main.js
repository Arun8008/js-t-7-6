/*----------6--------*/

const numbers = [1, 2, 3, 4, 5];
			const mul = numbers.reduce(function (v1, v2) {
			arun= v1 * v2;
				  return arun;
				});
			console.log(mul/3);
			document.write("<br>",mul/3);